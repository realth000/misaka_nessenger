/// Store all routes used in app.
class MisakaRoutes {
  /// Transport page, send files to other devices.
  static const scaffoldPage = '/';

  /// Page that shows when MisakaNessengerServer not started during startup.
  static const serverErrorPage = '/server_error';
}
