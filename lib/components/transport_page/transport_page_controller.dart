import 'package:get/get.dart';

/// Controller of transport page.
///
/// Transport page is a navigation bar (desktop) or tab bar (mobil).
class TransportPageController extends GetxController {
  /// Current index of transport page.
  var currentIndex = 0.obs;
}
